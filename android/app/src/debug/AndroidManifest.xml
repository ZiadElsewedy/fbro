<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- DEBUG-ONLY: allow plain-HTTP (cleartext) traffic so a physical device
         can reach the local dev backend at http://<mac-lan-ip>:3000. Android 9+
         blocks cleartext by default. Scoped to debug builds via this source
         set, so release builds keep cleartext disabled. -->
    <application
        android:usesCleartextTraffic="true"
        tools:targetApi="28"
        tools:ignore="UnusedAttribute" />
</manifest>
